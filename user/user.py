class User():
    def __init__(self) -> None:
        pass

    def get(id = some_number):
        user = 0 #* get from database
        return user

    def delete(user_id):
        #*delete user of that id
        pass #* return status code

    def modify(user_id, other_stuff):
        #*update user accordingly
        #*if NOT exist give out error
        pass #*return status code

    def add(user_id, other_stuff): #?user_id maybe add automatically by database
        #*add user
        #* if exist give out error
        pass #*return status code